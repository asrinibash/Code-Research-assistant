version: '3.8'

services:
  research-assistant:
    build: .
    container_name: code-research-assistant
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./:/app